apiVersion: v1
kind: Service
metadata:
  name: eify-backend
  namespace: eify
  labels:
    app: eify
    component: backend
spec:
  type: ClusterIP
  selector:
    app: eify
    component: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
