apiVersion: v1
kind: Service
metadata:
  name: eify-frontend
  namespace: eify
  labels:
    app: eify
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: eify
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
